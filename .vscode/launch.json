{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run current Python file",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}